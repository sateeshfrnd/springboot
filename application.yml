server:
  port: 8080
  
spring:
  datasource:
    #driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/products_db?useSSL=false
    username: root
    password: password
  jpa:    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:      ddl-auto: update      